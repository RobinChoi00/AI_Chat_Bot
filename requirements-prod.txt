# requirements-prod.txt
# =====================
# FastAPI backend (Docker / production) only.
#
# Used by Dockerfile.backend — NOT the full dev freeze in requirements.txt.
#
# Excluded (dev / legacy only):
#   torch, transformers, sentence-transformers, scipy, scikit-learn
#   streamlit, extra-streamlit-components
#   python-docx, pyarrow, pillow, altair, …
#
# pandas + openpyxl: Fonz xlsx → JSON ingest on server (script/ingest_fonz_warranty.py)

# --- Web API ---
fastapi==0.135.1
uvicorn==0.42.0
starlette==1.3.1
pydantic==2.12.5
pydantic-settings==2.14.2
python-multipart==0.0.31

# --- Config & HTTP ---
python-dotenv==1.2.2
requests==2.33.0

# --- Database ---
SQLAlchemy==2.0.48
pytz==2026.1.post1

# --- OpenAI + LangChain RAG ---
openai==2.29.0
langchain-openai==1.1.14
langchain-community==0.4.1
langchain-classic==1.0.7
langchain-core==1.3.3
langchain-text-splitters==1.1.2
tiktoken==0.12.0

# --- Vector search (OpenAI embeddings + FAISS; no local PyTorch models) ---
faiss-cpu==1.13.2
numpy==2.4.3
rank-bm25==0.2.2

# --- Fonz / Excel ingest (ingest_fonz_warranty.py on EC2) ---
pandas==2.3.3
openpyxl==3.1.5

# --- Rate limiting, retries, cache ---
slowapi==0.1.9
tenacity==9.1.4
cachetools==7.0.5

# --- RingCentral JWT (phone IVR) ---
PyJWT==2.13.0
cryptography==49.0.0
